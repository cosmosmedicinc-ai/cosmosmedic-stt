name: medibridge_realtime
description: Minimal hands-free realtime translation MVP.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_pcm_sound: ^3.3.3
  http: ^1.6.0
  permission_handler: ^12.0.1
  record: ^6.2.0
  web_socket_channel: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
