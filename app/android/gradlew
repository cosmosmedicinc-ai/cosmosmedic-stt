#!/usr/bin/env sh
set -e

if command -v gradle >/dev/null 2>&1; then
  exec gradle "$@"
fi

echo "Gradle is not installed. Run 'flutter create . --platforms=android' from app/ to regenerate the standard Android wrapper." >&2
exit 1
